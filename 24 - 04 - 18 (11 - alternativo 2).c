#include <stdio.h>

int main(){ // triangulo de Floyd solução da Taysa.

int i, j, n, soma = 0;

scanf("%d", &n);

for (i = 1; i <= n; i++){

	for (j = 1; j <= i; j++{
		soma += 1;
		printf("%d", soma);
	}
	
	print("\n");
}

return 0;

}