*/
/*int main() //(Soma dos inversos dos números)
{
    int i, n;
    float soma = 0.0;

    printf("Digite o numero de interesse: ");
    scanf("%d", &n);

    for(i=1; i<=n; i++) {
        soma = soma + (1/(float)i);
    }
    printf("A soma das frações inversas aos seus antecessores eh: %f", soma);

    return 0;
}
*/